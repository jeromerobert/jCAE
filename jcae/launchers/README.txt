See apisupport.harness/windows-launcher-src in the Netbeans source to rebuild Windows launchers. (require Cygwin).

To build cross build jcae.exe on Linux:
- copy files in this directory to apisupport.harness/windows-launcher-src
- run make-jcae.sh
- Add missing #include<stdio.h> until it build

