This suite requires Gnu Trove (http://trove4j.sourceforge.net)

It must be declared as library in netbeans under the name "trove".
This can be done either by using the IDE (menu Tools, Libraries) or by setting this property:
- libs.trove.classpath
in $HOME/.netbeans/6.0/build.properties
