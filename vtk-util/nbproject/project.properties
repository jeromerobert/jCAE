application.title=jCAE VTK Util
application.vendor=jCAE
build.classes.dir=${build.dir}/classes
build.classes.excludes=**/*.java,**/*.form
# This directory is removed when the project is cleaned:
build.dir=build
build.generated.dir=${build.dir}/generated
# Only compile against the classpath explicitly listed here:
build.sysclasspath=ignore
build.test.classes.dir=${build.dir}/test/classes
build.test.results.dir=${build.dir}/test/results
debug.classpath=\
    ${run.classpath}
debug.test.classpath=\
    ${run.test.classpath}
# This directory is removed when the project is cleaned:
dist.dir=dist
dist.jar=${dist.dir}/vtk-util.jar
dist.javadoc.dir=${dist.dir}/javadoc
excludes=
file.reference.VTK-java=../../../lib/VTK/java
file.reference.VTK.har=../../../lib/VTK/java/VTK.har
includes=**
jar.compress=false
javac.classpath=\
    ${libs.VTK.classpath}:\
    ${libs.trove.classpath}:\
    ${reference.amibe.jar}:\
    ${reference.occjava.jar}
# Space-separated list of extra javac options
javac.compilerargs=
javac.deprecation=false
javac.source=1.5
javac.target=1.5
javac.test.classpath=\
    ${javac.classpath}:\
    ${build.classes.dir}:\
    ${libs.junit.classpath}:\
    ${libs.junit_4.classpath}
javadoc.additionalparam=
javadoc.author=false
javadoc.encoding=${source.encoding}
javadoc.noindex=false
javadoc.nonavbar=false
javadoc.notree=false
javadoc.private=false
javadoc.splitindex=true
javadoc.use=true
javadoc.version=false
javadoc.windowtitle=
# Property libs.junit_4.classpath is set here just to make sharing of project simpler.
# The library definition has always preference over this property.
libs.junit_4.classpath=../../../netbeans-6.0.1/java1/modules/ext/junit-4.1.jar
# Property libs.trove.classpath is set here just to make sharing of project simpler.
# The library definition has always preference over this property.
libs.trove.classpath=../../../lib/trove-2.0.2/output/lib/trove-2.0.2.jar
# Property libs.VTK.classpath is set here just to make sharing of project simpler.
# The library definition has always preference over this property.
libs.VTK.classpath=../../paraview-3.2.1/install-vtk/lib/vtk.jar
main.class=org.jcae.vtk.test.TestUNV
manifest.file=manifest.mf
meta.inf.dir=${src.dir}/META-INF
platform.active=default_platform
project.amibe=../netbeans-suite/amibe
project.occjava=../netbeans-suite/occjava
reference.amibe.jar=${project.amibe}/dist/amibe.jar
reference.occjava.jar=${project.occjava}/dist/occjava.jar
run.classpath=\
    ${javac.classpath}:\
    ${build.classes.dir}
run.test.classpath=\
    ${javac.test.classpath}:\
    ${build.test.classes.dir}
source.encoding=US-ASCII
src.dir=src
