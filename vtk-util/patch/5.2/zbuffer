Don't clear z buffer during an area pick for consistancy with point pick. See bug 7236.
This patch has been added to the cvs head.
---

 Rendering/vtkOpenGLRenderWindow.cxx |    7 +++++++
 1 files changed, 7 insertions(+), 0 deletions(-)


diff --git a/Rendering/vtkOpenGLRenderWindow.cxx b/Rendering/vtkOpenGLRenderWindow.cxx
index be629f9..91d45c2 100644
--- a/Rendering/vtkOpenGLRenderWindow.cxx
+++ b/Rendering/vtkOpenGLRenderWindow.cxx
@@ -1265,6 +1265,9 @@ int vtkOpenGLRenderWindow::SetRGBACharPixelData(int x1, int y1, int x2,
   glMatrixMode( GL_MODELVIEW );
   glPopMatrix();
 
+  // Disable writing on the z-buffer.
+  glDepthMask(GL_FALSE);
+  glDisable(GL_DEPTH_TEST);
 
   glDisable( GL_SCISSOR_TEST );
   
@@ -1285,6 +1288,10 @@ int vtkOpenGLRenderWindow::SetRGBACharPixelData(int x1, int y1, int x2,
                   data);
     }
 
+  // Renenable writing on the z-buffer.
+  glDepthMask(GL_TRUE);
+  glEnable(GL_DEPTH_TEST);
+
   // This seems to be necessary for the image to show up
   glFlush();  
 
